{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "noEmit": false,
    "outDir": "./dist",
    "declaration": true,
    "declarationMap": true,
    // Add other relevant options if needed, e.g., module, target
    "baseUrl": ".",
    "rootDir": "src" // Assuming source code is in src/
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
} 